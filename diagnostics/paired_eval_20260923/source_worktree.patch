diff --git a/scripts/train.md b/scripts/train.md
index 0f9d74c..65ec171 100644
--- a/scripts/train.md
+++ b/scripts/train.md
@@ -25,7 +25,7 @@ BASE_LM_CE_WEIGHT=0.0 \
 MARKOV_HEAD_TYPE=rnn_easy \
 MARKOV_OUTPUT_MODE=direct \
 ENABLE_BACKBONE_CONV=true \
-export BACKBONE_CONV_MODE=simple_conv \
+BACKBONE_CONV_MODE=simple_conv \
 BACKBONE_CONV_KERNEL_SIZE=2 \
 BACKBONE_CONV_GROUP_SIZE=16 \
 MARKOV_RANK=512 \
@@ -74,7 +74,7 @@ latent 处注入；训练与推理均使用相同的 `0..block_size-2` slot 编
 
 训练时 target 冻结，只捕获 dense 历史所需的首层、中层、末层，以及当前 CHS 和 TV loss 所需层。draft 不使用 KV cache。
 
-### 层间卷积 simple_conv
+### Attention 前后卷积 simple_conv
 
 保留原有训练命令的其它参数，设置 `BACKBONE_CONV_MODE=simple_conv`。
 可选模式为 `none`、`full`、`simple_conv`；显式模式优先于旧的
@@ -92,8 +92,11 @@ bash scripts/run_training_flashmtp.sh --dt h100
 这里的 `--dt` 沿用原有环境配置选择，不根据 GPU 型号自动切换；数据、模型和输出路径
 需沿用自己的训练配置。
 
-`simple_conv` 在每个非末层的 MLP 残差相加之后执行一次单侧动态因果卷积，
-最后一层不加。5 层时共 4 次卷积，基础核恒等初始化、动态投影零初始化。
+`simple_conv` 保留原 full 模式每层 attention 输入与输出的动态因果卷积，
+删除 MLP 输入与输出的卷积，也不再添加层输出卷积。最后一层同样保留 attention 卷积。
+5 层时共 10 次卷积（full 为 20 次），基础核恒等初始化、动态投影零初始化。
+新 checkpoint 记录 `simple_conv_layout=attention_pre_post`；旧层间 output_conv 版
+simple_conv checkpoint 不能直接恢复，会明确报错，需要重新训练或显式转换权重。
 模式保存到 checkpoint 的 `flashmtp_config.backbone_conv_mode`，推理自动读取。
 旧 checkpoint 未记录模式时，根据 `backbone_conv_enabled` 恢复 none/full。
 恢复训练要求卷积模式相同，不能将已有 full checkpoint 直接作为 simple_conv resume。
diff --git a/specforge/modeling/draft/flashmtp.py b/specforge/modeling/draft/flashmtp.py
index 883da07..ea91e30 100644
--- a/specforge/modeling/draft/flashmtp.py
+++ b/specforge/modeling/draft/flashmtp.py
@@ -1,3 +1,4 @@
+import os
 import time
 from typing import Callable, Optional
 
@@ -506,12 +507,6 @@ class Qwen3FlashMTPDecoderLayer(GradientCheckpointingLayer):
         self.post_attention_layernorm = Qwen3RMSNorm(
             config.hidden_size, eps=config.rms_norm_eps
         )
-        self.output_conv = (
-            FlashMTPGroupedConv(
-                config.hidden_size, block_size=query_block_size,
-                taps=conv_kernel_size, group_size=conv_group_size, sides=1,
-            ) if simple_conv_enabled else None
-        )
         self.attention_conv = (
             FlashMTPGroupedConv(
                 config.hidden_size,
@@ -519,7 +514,7 @@ class Qwen3FlashMTPDecoderLayer(GradientCheckpointingLayer):
                 taps=conv_kernel_size,
                 group_size=conv_group_size,
             )
-            if backbone_conv_enabled
+            if backbone_conv_enabled or simple_conv_enabled
             else None
         )
         self.mlp_conv = (
@@ -577,8 +572,6 @@ class Qwen3FlashMTPDecoderLayer(GradientCheckpointingLayer):
         if self.mlp_conv is not None:
             hidden_states = self.mlp_conv.finish(hidden_states, mlp_kernel)
         hidden_states = residual + hidden_states
-        if self.output_conv is not None:
-            hidden_states = self.output_conv(hidden_states)
         return hidden_states
 
 
@@ -586,6 +579,28 @@ class FlashMTPDraftModel(Qwen3PreTrainedModel):
     config_class = Qwen3Config
     _no_split_modules = ["Qwen3FlashMTPDecoderLayer"]
 
+    @classmethod
+    def from_pretrained(cls, pretrained_model_name_or_path, *model_args, **kwargs):
+        # The previous simple_conv used inter-layer output_conv weights, which
+        # cannot initialize the new attention input/output wrappers.
+        config = kwargs.get("config")
+        if config is None or isinstance(config, (str, os.PathLike)):
+            config = cls.config_class.from_pretrained(
+                config if config is not None else pretrained_model_name_or_path,
+                **{k: kwargs[k] for k in (
+                    "cache_dir", "force_download", "local_files_only", "token",
+                    "revision", "subfolder",
+                ) if k in kwargs},
+            )
+        fcfg = getattr(config, "flashmtp_config", {}) or {}
+        if (fcfg.get("backbone_conv_mode") == "simple_conv"
+                and fcfg.get("simple_conv_layout") != "attention_pre_post"):
+            raise ValueError(
+                "Legacy simple_conv checkpoint uses output_conv and is incompatible "
+                "with attention_pre_post simple_conv; retrain or explicitly convert weights."
+            )
+        return super().from_pretrained(pretrained_model_name_or_path, *model_args, **kwargs)
+
     @torch.no_grad()
     def _init_weights(self, module: nn.Module) -> None:
         super()._init_weights(module)
@@ -743,6 +758,11 @@ class FlashMTPDraftModel(Qwen3PreTrainedModel):
             raise ValueError(f"Unknown backbone_conv_mode={self.backbone_conv_mode!r}")
         self.backbone_conv_enabled = self.backbone_conv_mode != "none"
         flashmtp_config["backbone_conv_mode"] = self.backbone_conv_mode
+        if self.backbone_conv_mode == "simple_conv":
+            layout = flashmtp_config.get("simple_conv_layout", "attention_pre_post")
+            if layout != "attention_pre_post":
+                raise ValueError(f"Unsupported simple_conv_layout={layout!r}")
+            flashmtp_config["simple_conv_layout"] = layout
         self.conv_kernel_size = int(flashmtp_config.get("conv_kernel_size", 2))
         self.conv_group_size = int(flashmtp_config.get("conv_group_size", 16))
         flashmtp_config["backbone_conv_enabled"] = self.backbone_conv_enabled
@@ -773,8 +793,7 @@ class FlashMTPDraftModel(Qwen3PreTrainedModel):
                     conv_kernel_size=self.conv_kernel_size,
                     conv_group_size=self.conv_group_size,
                     backbone_conv_enabled=self.backbone_conv_mode == "full",
-                    simple_conv_enabled=(self.backbone_conv_mode == "simple_conv"
-                                         and layer_idx < config.num_hidden_layers - 1),
+                    simple_conv_enabled=self.backbone_conv_mode == "simple_conv",
                 )
                 for layer_idx in range(config.num_hidden_layers)
             ]
@@ -1003,7 +1022,7 @@ class FlashMTPDraftModel(Qwen3PreTrainedModel):
         self.config.block_size = int(block_size)
         if self.backbone_conv_enabled:
             for layer in self.layers:
-                for conv in (layer.attention_conv, layer.mlp_conv, layer.output_conv):
+                for conv in (layer.attention_conv, layer.mlp_conv):
                     if conv is not None:
                         conv.set_block_size(self.draft_query_length)
 
diff --git a/tests/test_flashmtp_simple_conv.py b/tests/test_flashmtp_simple_conv.py
index 3f96806..5b1da5d 100644
--- a/tests/test_flashmtp_simple_conv.py
+++ b/tests/test_flashmtp_simple_conv.py
@@ -1,4 +1,6 @@
 import copy
+import json
+from pathlib import Path
 import tempfile
 import unittest
 import torch
@@ -14,22 +16,23 @@ class SimpleConvTest(unittest.TestCase):
     def test_identity_gradients_and_roundtrip(self):
         model=FlashMTPDraftModel(self.config())
         base=FlashMTPDraftModel(self.config('none'))
-        base.load_state_dict({k:v for k,v in model.state_dict().items() if '.output_conv.' not in k})
+        base.load_state_dict({k:v for k,v in model.state_dict().items() if '.attention_conv.' not in k})
         q=model.draft_query_length; c=model.condition_slot_count
         args=dict(position_ids=torch.arange(q)[None],rotary_position_ids=torch.arange(c+q)[None],noise_embedding=torch.randn(1,q,16),target_hidden=torch.randn(1,1,c,16),attention_mask=torch.zeros(1,1,q,c+q))
         y=model(**args); torch.testing.assert_close(y,base(**args),rtol=0,atol=0)
         y.mul(torch.randn_like(y)).sum().backward()
-        self.assertIsNone(model.layers[-1].output_conv)
-        for l in model.layers[:-1]:
-            self.assertIsNone(l.attention_conv); self.assertIsNone(l.mlp_conv)
-            self.assertEqual(l.output_conv.kernel_projection.out_features,8)
-            self.assertGreater(l.output_conv.kernel_projection.weight.grad.abs().sum().item(),0)
+        for l in model.layers:
+            self.assertIsNotNone(l.attention_conv)
+            self.assertIsNone(l.mlp_conv)
+            self.assertFalse(hasattr(l, 'output_conv'))
+            self.assertEqual(l.attention_conv.kernel_projection.out_features,16)
+            self.assertGreater(l.attention_conv.kernel_projection.weight.grad.abs().sum().item(),0)
         with tempfile.TemporaryDirectory() as d:
             model.save_pretrained(d); restored=FlashMTPDraftModel.from_pretrained(d)
             self.assertEqual(restored.backbone_conv_mode,'simple_conv')
             torch.testing.assert_close(restored(**args),y)
         model.set_config_block_size(6)
-        for l in model.layers[:-1]: self.assertEqual(l.output_conv.block_size,model.draft_query_length)
+        for l in model.layers: self.assertEqual(l.attention_conv.block_size,model.draft_query_length)
     def test_boundary_and_one_layer(self):
         conv=FlashMTPGroupedConv(4,3,2,2,sides=1)
         x=torch.arange(24.).reshape(1,6,4)
@@ -37,7 +40,29 @@ class SimpleConvTest(unittest.TestCase):
         with torch.no_grad(): conv.base_kernel.zero_(); conv.base_kernel[:,1]=1
         expected=torch.zeros_like(x); expected[:,1:3]=x[:,:2]; expected[:,4:6]=x[:,3:5]
         torch.testing.assert_close(conv(x),expected)
-        self.assertIsNone(FlashMTPDraftModel(self.config(layers=1)).layers[0].output_conv)
+        self.assertIsNotNone(FlashMTPDraftModel(self.config(layers=1)).layers[0].attention_conv)
+    def test_matches_full_with_mlp_convolutions_removed(self):
+        full=FlashMTPDraftModel(self.config('full'))
+        for layer in full.layers:
+            layer.mlp_conv=None
+            torch.nn.init.normal_(layer.attention_conv.kernel_projection.weight,std=0.02)
+            torch.nn.init.normal_(layer.attention_conv.base_kernel,std=0.2)
+        simple=FlashMTPDraftModel(self.config())
+        simple.load_state_dict(full.state_dict(),strict=True)
+        q=simple.draft_query_length; c=simple.condition_slot_count
+        args=dict(position_ids=torch.arange(q)[None],rotary_position_ids=torch.arange(c+q)[None],noise_embedding=torch.randn(1,q,16),target_hidden=torch.randn(1,1,c,16),attention_mask=torch.zeros(1,1,q,c+q))
+        torch.testing.assert_close(simple(**args),full(**args),rtol=0,atol=0)
+        self.assertTrue(all(l.mlp_conv is not None for l in FlashMTPDraftModel(self.config('full')).layers))
+    def test_rejects_legacy_simple_checkpoint(self):
+        model=FlashMTPDraftModel(self.config())
+        with tempfile.TemporaryDirectory() as d:
+            model.save_pretrained(d)
+            path=Path(d)/'config.json'
+            config=json.loads(path.read_text())
+            del config['flashmtp_config']['simple_conv_layout']
+            path.write_text(json.dumps(config))
+            with self.assertRaisesRegex(ValueError,'Legacy simple_conv'):
+                FlashMTPDraftModel.from_pretrained(d)
     def test_invalid_mode(self):
         with self.assertRaisesRegex(ValueError,'backbone_conv_mode'): FlashMTPDraftModel(self.config('invalid'))
 
