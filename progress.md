我现在在做一个投机解码的工作，在和一个工作（sota）比较。这个工作将全部前文的大模型hiddenstates拼接投影作为kvcache，一次预测后面B个token。

我认为传统的投机解码，草稿模型自回归太慢了。文字之间语义是连贯的，相关的，因此我可以利用双向注意力/扩散原理来进行少次前向就生成一段长度的候选token。

并且，我认为对于草稿模型（上面的sota），kvcache是冗余的，我的insight是在长上下文下，全量kvcache会对小模型造成挑战。因此我只输入给小模型一个浓缩的特征向量应该就能预测。大模型生成的最新的隐藏状态应该是计算了所有历史信息，因此我应该可以只使用这个信息就可以预测。

此外，我现在实现了基础结构，把最新的大模型每层hiddenstates拼接，投影成一个embedding，在拼接B个mask，一次forward，双向注意力，直接生成cleantoken，大模型再去验证。训练的时候也是这样训练，loss是每个位置的交叉熵，并且越前面的token loss权重越大。可是基础结构效果不如保持全量的kvcache。我现在在想如何改进我的模型原理或者架构，请帮我分析一下。对于草稿模型设计，那些是变量我可以优化的.

我的核心就是去掉kvcache。请不要变动并且相信大模型最新hiddenstates信息足够。并且，对于大模型每层，关注的历史token是不同的，不同层hiddenstates应该已经包含了token的交互，只是我没有利用好，请基于此继续思考

v2版本：
我将targethidden在序列维度进行拼接，并加入layerembed投影建模层序。效果微微好了一点。但还是和sota有一定差距。
* 发现：我没有使用全部层，二十挑选了收尾两层和中间均匀采样五层，效果一样。

请问我现在应该做哪些实验，有哪些可能改进的点，可以超过这个工作。

我现在有两个问题：
1. 是不是只保留最新hs信息不够丰富。这个hs对于前文token关注度有大有小，可能他只关注了一部分重要token。而如果要生成后续B个token，可能每个token要关注的前文都不同，会对信息造成稀释。
2. 全量kvcache真的会对小参数模型造成挑战吗。
