# FlashMTP

## Background
我现在在做一个投机解码的工作。

**传统的投机解码**：草稿模型是自回归的太慢了。然而文字之间语义是连贯的，相关的，我的目标是进行词组的预测。词组之间是强相关的，因此我利用双向注意力，输入多个mask，希望一次预测多个token出来。

**KV cache抛弃**： 对于草稿模型，kvcache是冗余的。大模型***生成的最新的隐藏状态***应该是计算了所有历史信息，理论上是对前文的浓缩。因此我将这个作为上下文中枢（Contextual Pivot）可以只使用这个信息就可以预测后面一块内容。此外，大模型不同深度的层关注前文不同的信息，因此我会纳入所有层的hidden states，进行信息提取。


### 核心
我的核心就是去掉kvcache。请不要变动并且相信大模型最新hiddenstates信息足够。并且，对于大模型每层，关注的历史token是不同的，不同层hiddenstates应该已经包含了token的交互，只是我没有利用好，请基于此继续思考

### 定义
* Contextual Pivot (上下文枢轴)：目标模型最新的融合hidden sstates，它是连接过去（全量历史）与未来（生成块）的支点。
* hs：hidden states的简称。hs可以是任意层的输出hs
* 训练数据：我的训练数据全部是目标大模型生成的响应，这样可以对齐。
* anchor token：训练时随机采样的位置上的token，训练输入为，预测anchortoken的hs（即pivot），拼接ancho token（clean的）在拼接上B-1个noise embedding。


## 实验规划
我现在对于现有思路和method细节进行试验  
具体包括：
1. 对于target mode hiden states，取的层数越多是否越好。选取策略：固定首尾两层输出hiddenstates，中间等间隔选取N层。这里N在脚本中给出接口可以修改
2. 对于target hidden融合方式。有三种：1. 现有选定层沿着特征维度拼接，linear的定权融合成pivot 2. 用attention提取信息，用最后一个hiddenstates去attend之前层（这里的位置编码直接是用0，1，2.。。自然数），再加一个残差，取最后一个位置出来作为pivot，这是input-aware融合。 3.用prefix conditioning，将 HS 拼接在anchor和mask输入序列的最前面。注意草稿模型每层都要将hs重新拼接进去作为条件。这里，从anchor开始到后面的mask用原本的位置编码作为query。hs不要加rope形式位置编码，而是加上可学习的层深度编码，层深度就用自己在target模型中的层idx。 这三种的target hidden的选择策略和第一个一样：选取策略：固定首尾两层输出hiddenstates，中间等间隔选取N层。这里N在脚本中给出接口可以修改。同时，这三种方法依次命名为linear_fuse;attention_fuse和prefix_condition，可以在/data/wanghanzhen/Projects/MTP/NIPS26/FlashMTP_v1.1/scripts/run_training_flashmtp.sh脚本中进行切换。且模型命名，wandb run id包括方法名和N的值，方便我做ablation study。

### prefix_condition：`target_hidden` 如何取出与如何注入草稿（对照代码）

以下内容对应 `specforge/core/flashmtp.py`（训练）与 `specforge/modeling/draft/flashmtp.py`（草稿前向）。

**1. Target hidden 从哪里来、张量形状**

- 对每个选中的 transformer 层 `target_layer_ids[k]`（0-based），在序列上取 **anchor 前一时刻**的位置：$\mathrm{context\_pos}=\max(\mathrm{anchor}-1,\,0)$。训练时用 `prepare_target_hidden`：对每层从该 batch 整条序列里 `gather` 出向量；同一 batch 里每个 parallel block（每个 anchor）各有一份，故形状为 **`(B, N_blocks, S_layers, H)`**，其中 $S$ 为选取的 target 层数（与首尾+N 中间的策略一致）。
- 推理时用 `gather_pivot_multilayer_inference`，在单行序列上对某一 pivot 索引（例如 block 末尾或接受后的 pivot）取多层 hidden，形状为 **`(B, 1, S_layers, H)`**，再送进同一套草稿前向。

**2. 进入草稿前的预处理（depth + norm，不叫“拼接 token 向量”而是通过长度扩成多条 KV）**

- `pivot_fuse_mode=="prefix_condition"` 时，`FlashMTPDraftModel._fuse_target_hidden` 对每个 target 层向量加上 **`nn.Embedding(num_target_layers, H)`** 里由该层在 target 中的 **层号 `target_layer_ids[k]`** 查出的向量（可学习的“深度编码”，**不是 RoPE**），再 `RMSNorm`，最后把 **`(B, N, S, H)` reshape 成 `(B, N*S, H)`**。因此每个 block 在 attention 侧对应 **`S` 条“上下文槽位”**，总上下文长度 **`chs_len_per_block = S`**（与其它两种 fuse 时为 1 不同）。
- 草稿每一层 `Qwen3FlashMTPDecoderLayer` **都传入同一份** `target_hidden`（每层 self-attn 都从这份 KV 前缀读条件，而不是仅第一层）。

**3. 真正“注入”方式：attention 里的 KV 前缀 + 只对主序列施加 RoPE**

- 草稿输入仍是 **noise embedding**：每个 block「anchor 的真实 token embedding + 后续 mask embedding」，形状 **`(B, N*block_size, H)`**。
- Rotary：对整段「上下文 + 草稿块」拼接 **绝对 position id**（训练：`anchor-1` 重复 $S$ 次后接 anchor…anchor+block_size−1）；用 `dummy` 过 `rotary_emb` 只生成 cos/sin。这样 RoPE 表长度是 **`N*S + N*block_size`**。
- **`Qwen3FlashMTPAttention` 中 `prefix_condition` 分支**：
  - **Q** 与 **草稿序列的 K/V**（由 `noise_embedding` 经 `k_proj`/`v_proj`）先做 **RoPE**（取用上述 cos/sin 中与草稿段对齐的那段，即整块序列的最后 `q_len` 位置）。
  - **Target hidden** 先经 **`k_proj`/`v_proj` 得到 K_ctx、V_ctx**，**不参与 RoPE**。
  - 在 head 维度上把时间维 **`K = cat([K_ctx, K_noise], dim=seq)`，`V = cat([V_ctx, V_noise], dim=seq)`**（前缀是 target hidden，后缀是 draft token）。Attention 对每个 query position 会看到 **长度为 `S + block_size` 的 KV**。
- **训练全局 mask**（`create_flashmtp_block_mask`）：每个 block 的 Q 只允许 attend **自己的** $S$ 个 context slot + **自己的**整块 draft（块间互不可见），与 KV 前缀布局一致。
- **`linear_fuse` / `attention_fuse`** 的差异：fuse 先把多层压成 **每条序列 1 个 pivot 向量**，再与 draft 一起做 RoPE（**非 `prefix_condition` 分支**会把 context 与 draft 拼接后的整块 `k`/`v` 一起过 RoPE）。**prefix 模式刻意把前半段 KV 留在 RoPE 之外**，只用深度 embedding 区分层身份。

简述：**prefix_condition 并不是把 HS 当成普通 token embedding 再接进 RMSNorm→QKV 的同一路径**，而是把它们 **映射为额外的 K/V 前缀**；草稿 token 仍为正常 RoPE queries/keys；"target 各层在序列维上展开为多槽 + 深度 embedding + 无前缀 RoPE" 即为该模式的注入语义。