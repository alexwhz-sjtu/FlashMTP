# FlashMTP

## Background
我现在在做一个投机解码的工作。

**传统的投机解码**：草稿模型是自回归的太慢了。然而文字之间语义是连贯的，相关的，我的目标是进行词组的预测。词组之间是强相关的，因此我利用双向注意力，输入多个mask，希望一次预测多个token出来。

**KV cache抛弃**： 对于草稿模型，kvcache是冗余的。大模型***生成的最新的隐藏状态***应该是计算了所有历史信息，理论上是对前文的浓缩。因此我将这个作为上下文中枢（Contextual Pivot）可以只使用这个信息就可以预测后面一块内容。此外，大模型不同深度的层关注前文不同的信息，因此我会纳入所有层的hidden states，进行信息提取。


### 核心
我的核心就是去掉kvcache。请不要变动并且相信大模型最新hiddenstates信息足够。并且，对于大模型每层，关注的历史token是不同的，不同层hiddenstates应该已经包含了token的交互，只是我没有利用好，请基于此继续思考

### 定义
* Contextual Pivot (上下文枢轴)：目标模型最新的融合hidden sstates，它是连接过去（全量历史）与未来（生成块）的支点。
* hs：hidden states的简称。hs可以是任意层的输出hs
* 训练数据：我的训练数据全部是目标大模型生成的响应，这样可以对齐。
* anchor token：训练时随机采样的位置上的token，训练输入为，预测anchortoken的hs（即pivot），拼接ancho token（clean的）在拼接上B-1个noise embedding。


### prefix_condition：target hidden 的取出与注入

**取出 pivot**

- 在 target 模型选定的若干层上，取 **anchor 前一位置** $\max(\mathrm{anchor}-1, 0)$ 的 hidden state。
- 每个 anchor block 得到 **$S$ 个向量**（$S$ = 选取的 target 层数），训练时 batch 内各 block 并行，推理时取当前 pivot 位置的多层 hidden。

**预处理：多层展开为 $S$ 个上下文槽**

- 每层向量加上可学习的 **层号 embedding**（区分深度，**不是 RoPE**），再 RMSNorm。
- 同一 block 的 $S$ 层向量在序列维 **展开成 $S$ 个槽位**，不再压成 1 个 pivot（`linear_fuse` / `attention_fuse` 才是压成 1 个）。
- 草稿 **每一层 decoder** 都读同一份展开后的条件，而非只在第一层注入。

**注入：K/V 前缀 + 草稿 token 后缀**

- 草稿主序列仍是每个 block 的 **anchor 真 token + 后续 mask embedding**。
- Target hidden 经 K/V 投影得到 **前缀 K/V**，**不加 RoPE**；草稿 token 经 Q/K/V 投影后 **正常加 RoPE**。
- Attention 里按时间维拼接：**`K = [K_ctx ∥ K_draft]`，`V = [V_ctx ∥ V_draft]`**。每个 draft 位置 attend 长度为 **$S + B$** 的 KV（$S$ 个 pivot 槽 + 块内 $B$ 个 draft 位置）。
- 块间互不可见：每个 block 的 query 只能看 **自己的** $S$ 个 context 槽 + **自己的** draft 块。

**与其它 fuse 模式的区别**

- `linear_fuse` / `attention_fuse`：多层先融合成 **1 个** pivot，再与 draft **整段一起做 RoPE**。
- `prefix_condition`：多层 **不融合**，在 KV 侧展开为 $S$ 个无前缀 RoPE 的槽位；draft 仍走常规 token 路径。HS 不是普通 token embedding 串进同一 QKV 流，而是 **单独映射为 attention 的 K/V 前缀**。

### 训练方法
#### base
直接用ce loss计算。

#### v1.3：DFlash 蒸馏（最终输出 + 可选中间层）

**动机**：FlashMTP 是 KV-free pivot 条件，DFlash 是全量历史 HS/KV 条件，二者在同一并行 block 上预测，空间可对齐。直接对齐 target 自回归分布会伤害并行 draft，因此用 DFlash 作 teacher，在同一组 anchor、`anchor clean + mask block`、同一组 slot 上蒸馏。

**共同设定**

- Teacher / student 共用同一 batch 的 target hidden、同一组采样 anchor 与 noise layout。
- 有效 slot：排除 anchor slot 0、越界 label、`loss_mask=0`、无效 block。
- KL 在 `teacher top-k + true label` 候选集上算，temperature 默认 2.0，top-k 默认 128。

---

##### 两种训练模式

| 模式 | 说明 |
|------|------|
| **final** | 只对齐最终输出（末层 hidden → lm_head → logits） |
| **final+mid** | 在 final 基础上，额外对齐草稿与 DFlash 的中间层 hidden |

两种模式对**最终输出**的监督规则相同；`final+mid` 再叠加中间层项与 epoch 调度（见下）。

---

##### 最终输出损失（两种模式共用）

**1. DFlash KL（teacher 正确 slot，位置等权）**

只在 DFlash top1 等于 true label 的 slot 上算 KL：

$$
g_k = \mathbf{1}[\arg\max p_{\text{DFlash},k} = y^*_k]
$$

$$
L_{\text{KL}} = \frac{\sum_k g_k \cdot \mathrm{KL}(p_{\text{DFlash},k} \Vert p_{\text{FlashMTP},k})}{\sum_k g_k + \epsilon}
$$

- **位置等权**：KL 分子分母均**不**乘 $\exp(-(k-1)/\gamma)$；每个参与 slot 权重相同。
- teacher 预测错误的 slot 不参与 KL（只吸收 teacher 可靠的 soft target）。

**2. True-label CE（全部有效 slot，保持现有降权）**

$$
w_k^{\text{CE}} = \exp\!\left(-\frac{\max(k-1,0)}{\gamma}\right)
$$

$$
L_{\text{CE}} = \frac{\sum_k w_k^{\text{CE}} \cdot \mathrm{CE}(p_{\text{FlashMTP},k},\, y^*_k)}{\sum_k w_k^{\text{CE}} + \epsilon}
$$

- 覆盖全部有效 draft slot（含 teacher 错、student 错的位置），$\gamma$ 沿用 `loss_decay_gamma`。
- 可设置 `DFLASH_CE_WRONG_WEIGHT` 对 teacher top1 错误 slot 的 CE 额外降权；例如 `0.2` 表示这类位置在现有 CE 位置权重基础上再乘 0.2。
- **与 KL 解耦**：CE 用 decay 权重，KL 用等权；不再共用同一 `weight_mask`。

---

##### 中间层对齐（仅 `final+mid`）

FlashMTP draft 与 DFlash teacher **层数相同**，记 $L = \texttt{num\_draft\_layers}$。对第 $\ell$ 层 decoder 输出的 block hidden（形状与末层相同，按 `(B, N, block\_size, H)` 理解）在同一 slot 上对齐。

**对齐形式**：Norm-Hidden MSE（同 slot、同层索引）。先对 teacher / student hidden 在特征维做归一化，再算 MSE，避免 raw hidden 尺度差异主导损失：

$$
\tilde h = \mathrm{norm}(h)
$$

$$
L_{\text{mid}}^{(\ell)} = \frac{1}{|\mathcal{S}_{\text{mid}}|}\sum_{k \in \mathcal{S}_{\text{mid}}} \|\, \tilde h^{(\ell)}_{\text{DFlash},k} - \tilde h^{(\ell)}_{\text{FlashMTP},k} \,\|_2^2
$$

$\mathcal{S}_{\text{mid}}$ = teacher top1 正确的有效 slot 集合：

$$
\mathcal{S}_{\text{mid}} = \{k \mid \mathrm{valid}_k=1,\ \arg\max p_{\text{DFlash},k}=y^*_k\}
$$

其中 $\mathrm{valid}_k$ 与 CE 的有效性定义相同（pos $\ge 1$，有效 block / label / loss_mask）。teacher 预测错误的位置不参与中间层 hidden 对齐，只交给 true-label CE 纠偏。

**子模式**

| 子模式 | 对齐层 | 聚合 |
|--------|--------|------|
| **full** | $\ell = 0, 1, \ldots, L-2$（**除最后一层外的每一层**） | $L_{\text{mid}} = \frac{1}{L-1}\sum_{\ell=0}^{L-2} L_{\text{mid}}^{(\ell)}$ |
| **half** | 仅 $\ell = \lfloor L/2 \rfloor$ | $L_{\text{mid}} = L_{\text{mid}}^{(\lfloor L/2 \rfloor)}$ |

- 末层 $L-1$ 已由 $L_{\text{KL}} + L_{\text{CE}}$ 监督，中间项**刻意不对齐末层**，避免与 logits 损失重复。
- full 对各中间层**等权**平均；half 只抽一层，计算更省、偏“中段表征”蒸馏。
- 例：$L=5$ 时 full 对齐层 0–3，half 对齐层 2。

---

##### 总损失与 epoch 调度

`final` 与 `final+mid` 都使用同一套 milestone + cosine 调度；区别只是 `final` 没有 $L_{\text{mid}}$。以 milestone epoch $m = \texttt{ep\_m}$ 分两阶段；milestone 之后使用 **Cosine Schedule（余弦调度）** 平滑切换。记 $t = \max(0,\, e - m) / (E_{\max} - m)$ 为 $m$ 之后归一化进度（$e$ 当前 epoch，$E_{\max}$ 总 epoch 数），并 clamp 到 $[0,1]$：

$$
c(t)=\frac{1+\cos(\pi t)}{2}
$$

若设置最小蒸馏保留比例 $s_{\min}=\texttt{DFLASH\_DISTILL\_MIN\_SCALE}$，则 KL / mid 的衰减系数为：

$$
d(t)=s_{\min} + (1-s_{\min})c(t)
$$

CE 也可设置最小保留比例 $r_{\min}=\texttt{DFLASH\_CE\_MIN\_SCALE}$：

$$
u(t)=r_{\min} + (1-r_{\min})(1-c(t))
$$

| 阶段 | $e < m$ | $e \ge m$ |
|------|---------|-----------|
| 蒸馏组 $L_{\text{distill}} = L_{\text{KL}} + \lambda_{\text{mid}}\, L_{\text{mid}}$ | 权重 $\lambda_{\text{kl}}, \lambda_{\text{mid}}$ 满值 | 按 $d(t)$ **余弦衰减到 floor** |
| CE | $\lambda_{\text{ce}}=\texttt{DFLASH\_CE\_WEIGHT}\cdot r_{\min}$ | 按 $u(t)$ **余弦上升**至目标值 |

$$
\mathcal{L} = \underbrace{\alpha_{\text{kl}}(e)\, L_{\text{KL}} + \alpha_{\text{mid}}(e)\, L_{\text{mid}}}_{\text{蒸馏组，} e\ge m \text{ 后衰减}} + \underbrace{\alpha_{\text{ce}}(e)\, L_{\text{CE}}}_{\text{从 CE floor 上升}}
$$

- **直觉**：前期用 DFlash 全量条件把 pivot 表征与中间层“逼出来”；$m$ 之后逐步交给 true-label CE 纠偏 acceptance，同时减弱 teacher 约束，避免过拟合 teacher 错误。
- 默认 $\alpha_{\text{kl}}, \alpha_{\text{mid}}$ 共用同一余弦衰减系数；`final` 模式中 $\alpha_{\text{mid}}=0$。$\alpha_{\text{ce}}$ 使用带 floor 的互补余弦上升系数，并最终达到 `DFLASH_CE_WEIGHT`。
- 当 `DFLASH_DISTILL_MIN_SCALE=0` 时，KL / mid 最终衰减到 0；例如设为 `0.1` 时，最终保留 10% 的 `DFLASH_DISTILL_WEIGHT` / `DFLASH_MID_WEIGHT`。
- 当 `DFLASH_CE_MIN_SCALE=0.2` 时，milestone 前 CE 权重为 `0.2 * DFLASH_CE_WEIGHT`，之后从该 floor 上升到完整 CE 权重。

---

##### 实现要点（待接入）

- Draft / DFlash forward 需 **`output_hidden_states=True`**，按层收集中间 hidden；mid loss 跟随 `DFLASH_DISTILL_POS_MODE` 选择监督位置，并对 hidden 做 norm 后再算 MSE。
- Teacher 仅推理，checkpoint 只存 FlashMTP draft。
- 建议 CLI / 环境变量：
  - `DFLASH_ALIGN_MODE`：`final` | `final+mid`
  - `DFLASH_MID_ALIGN`：`full` | `half`（仅 `final+mid`）
  - `DFLASH_MILESTONE_EPOCH`（`ep_m`）
  - `DFLASH_DISTILL_MIN_SCALE`：KL / mid 最终保留比例；`0.0` 表示完全衰减到 0，`0.2` 表示最终保留 20%
  - `DFLASH_CE_MIN_SCALE`：CE 初始 / 最小保留比例；`0.2` 表示 milestone 前 CE 权重为 `0.2 * DFLASH_CE_WEIGHT`
  - `DFLASH_DISTILL_POS_MODE`：`prefix` | `all`，共同控制 KL 与 hidden-state distill 的监督位置
  - `DFLASH_CE_POS_MODE`：`prefix` | `all`，单独控制 true-label CE 的监督位置
  - `DFLASH_CE_WRONG_WEIGHT`：`DFLASH_CE_POS_MODE=all` 时 teacher 错误 slot 的 CE 额外权重，例如 `0.2`
  - `DFLASH_DISTILL_WEIGHT`、`DFLASH_MID_WEIGHT`、`DFLASH_CE_WEIGHT`
  - milestone 切换处固定使用 cosine 衰减 / 上升 schedule


##### v1.3 位置模式

将 loss 行为拆成两组：

- `distill loss`：DFlash KL loss 与 hidden-state loss，二者使用同一种位置模式。
- `CE loss`：true-label CE，位置模式单独控制。

每组 loss 都有两种位置模式：

- `prefix`：只计算 teacher 预测与 label 连续最大匹配前缀的位置，仍排除 anchor 位置。
- `all`：计算全部有效 draft slot，仍排除 anchor 位置。

CE 的 `prefix` 模式额外加入一个探索位置：如果 draft 自己的连续正确长度已经等于或超过 teacher 的连续正确长度，则再向后监督 draft 的第一个错误位置。该额外位置只用于 CE，不用于 KL / hidden-state distill。

保持现有位置权重衰减行为：CE 使用 `LOSS_DECAY_GAMMA`，distill 使用 `DFLASH_DISTILL_DECAY_GAMMA`。当 CE 使用 `all` 模式时，只有 CE 对 teacher wrong slot 使用 `DFLASH_CE_WRONG_WEIGHT` 额外降权；distill 使用 `all` 时不做 teacher wrong 降权。
