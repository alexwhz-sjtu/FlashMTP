# FlashMTP v3.3 推进文档

## Background
我现在在做一个投机解码的工作。

**传统的投机解码**：草稿模型是自回归的太慢了。然而文字之间语义是连贯的，相关的，因此我可以利用双向注意力/扩散原理来进行少次（理想是1次）前向就生成一段长度的候选token。

**KV cache抛弃**： 对于草稿模型，kvcache是冗余的。大模型***生成的最新的隐藏状态***应该是计算了所有历史信息，理论上是对前文的浓缩。因此我将这个作为上下文中枢（Contextual Pivot）可以只使用这个信息就可以预测后面一块内容。此外，大模型不同深度的层关注前文不同的信息，因此我会纳入所有层的hidden states，进行信息提取。


### 核心
我的核心就是去掉kvcache。请不要变动并且相信大模型最新hiddenstates信息足够。

## Loss 设计（概念说明）

训练拆成两阶段，各自一个标量总目标；下面只说明「优化什么、在什么位置上算」，不涉及实现细节。

### 阶段一（MDLM）

- **目的**：让草案在「随机残缺块」条件下学会还原真实 token，为阶段二提供可用的初始化。
- **块与掩码**：在数据允许监督的区间内随机采多个块起点；每个块单独抽一层掩码率（可偏向高掩码）。块内第一个位置是 **anchor**，始终保留真实内容、不参与随机掩码；只有块内靠后的位置才会被随机挖成待预测槽。
- **交叉熵**：只在「被掩码且仍在合法监督范围内」的位置上，用草案分布对真实 token 做交叉熵；按实际参与的位置数做平均。**不再**使用随距块首变远的 loss 权重衰减。
- **可选 KL**：若开启，则在同一批被监督位置上，再让草案分布逼近教师（目标模型）的分布；可按教师高概率子空间近似以降低开销，也可在全词表上算。最终标量是 **加权的 CE 与加权的 KL** 之和，权重由训练配置决定。

### 阶段二（Streak）

- **目的**：在固定 pivot 条件下，抬高「验证器沿教师轨迹连续接受整块中多个草案 token」的联合质量，而不是只拟合第一个 token。
- **前向形态**：每个块随机 anchored；块首喂入真实 token 的嵌入，块内其余位置统一为掩码占位，草案一次性产出整块 logits。
- **相对 streak（LS-RSL）**：对每个被监督位置，用教师在 **真标签** 上的概率设 **锚点**（不低于一个常数下限，也不低于教师自己给真标签的概率）。把草案对真标签的概率相对该锚点写成「相对置信度」；在块内 **除块首外** 的位置上沿前缀做 streak 式的累积。若草案相对教师锚点已经「足够好」，主 streak 目标对该位置不再追加梯度，使优化重点留在仍偏弱的位置；块首不参与 streak 的外层前缀求和，与「anchor 只做条件、不当 streak 乘积起点」的设定一致。
- **稳定性**：真标签的对数概率在数值上设有下界，避免极端小概率导致累乘与对数不稳定。
- **streak 标量**：在每个有效块上比较「理想可接受前缀规模」与「模型给出的相对 streak 总和」的对数差，再对有效块平均；最小化该量等价于推高相对 streak 质量。
- **可选 CE 辅助**：若权重非零，则在同一套有效位置上叠加普通的逐 token 交叉熵，为 streak 提供逐点似然约束。总标量为 **streak 主项与 CE 辅助项的加权和**。
- **参与范围**：所有项都尊重数据上的监督掩码、序列不越界，以及当前 batch 中块采样是否有效；只累积数据管线里标为需要监督的 token 区间。


## 运行方式

v3.3 只保留两个自包含入口（各自文件顶部独立改参；`--dt qz|a800|h100` 与额外参数仍可透传到 Python）：

```bash
# 阶段一：从零 MDLM
./scripts/train_mdlm_v33.sh --dt a800

# 阶段二：Streak（随机初始化）
./scripts/train_streak_v33.sh --dt a800

# 阶段二：Streak（承接 MDLM 权重）
STREAK_FROM_SCRATCH=0 MDLM_INIT_CKPT=/path/to/mdlm_out/epoch_1_step_1000 ./scripts/train_streak_v33.sh --dt a800
```

也可在脚本里改 `STREAK_FROM_SCRATCH` / `MDLM_INIT_CKPT`。两阶段衔接：先跑 `train_mdlm_v33.sh`，再在 `train_streak_v33.sh` 中把 `MDLM_INIT_CKPT` 指到最新 `epoch_*_step_*` 目录（或上述环境变量）。

**说明**：原 `run_v3_3_lib.sh`、`run_v3_3_mdlm.sh`、`run_v3_3_streak.sh`、`run_v3_3_pipeline.sh`、`run_v3_3_training_flashmtp.sh` 与 v3.3 下的 `run_training_flashmtp.sh`（旧版 `train_flashmtp.py` 入口）已合并/移除；若仍需旧版非 MDLM 训练脚本，可参考其他版本目录中的 `run_training_flashmtp.sh`。

## 验证

- 冒烟：依次或单独跑 `train_mdlm_v33.sh` / `train_streak_v33.sh`（小数据可在脚本内改各 `TRAIN_DATA_PATH_*` 指向本地 jsonl）；Streak 中 `log_probs` 与 `target_ids` 的 gather 已对齐为 `(B, N*bs)` 再 `view(B,N,bs)`。

## 总结

v3.3 已将 **MDLM 预训练** 与 **Streak 蒸馏** 分为两个独立 Python 入口与两个 shell（`train_mdlm_v33.sh`、`train_streak_v33.sh`），共享数据构建、存盘与 FSDP 逻辑。技术上通过 **对齐 layer_types** 与 **仅权重加载** 解决了阶段间衔接的 config 校验问题。

## 待你本地确认

- a800 上真实 `TRAIN_DATA_PATH` 是否与 `qwen3-thinking` 模板一致；若数据格式不同可改 `CHAT_TEMPLATE` 或 `IS_PREFORMATTED`。
- 两个 shell 顶部默认超参可分别修改（如 `NUM_DRAFT_LAYERS`、`BLOCK_SIZE`）；冒烟时自行缩小 `NUM_EPOCHS`、`MAX_LENGTH` 或改用本地 jsonl 路径。
