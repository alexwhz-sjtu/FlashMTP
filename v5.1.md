# FlashMTP v5 修改总结

## 核心动机

v5.1 的出发点是承认 pure pivot 条件的信息量可能不足，但仍然坚持草稿侧不维护 KV cache。相比只在 `anchor-1` 单点上做多层 hidden fusion，v5.1 引入attention sink思想以及滑动窗口注意力。

## Hidden State 融合方法

使用现有hidden states选取和融合方法。等间隔抽取+fc降维

## Pivot选取

选择整个序列（包括prompt）最开始的两个位置的融合hs（即position 0，1），加上pivot以及往前的4个位置的融合hs作为草稿模型的条件。

